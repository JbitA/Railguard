listener 1883 0.0.0.0
allow_anonymous true

# QoS-1 is only a durable edge→cloud handoff if the broker itself persists queued
# messages for the ingestor's stable persistent session.
persistence true
persistence_location /mosquitto/data/
autosave_interval 5

log_dest stdout
